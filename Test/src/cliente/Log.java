package cliente;


/*
 * Funciones de la Clase:
 * Crear archivo de texto en la carpeta raiz del cliente para almacenar logs
 * Guardar todos los registros (A futuro encriptarlos)
 *
 * Implementar:
 * Métodos para dichas funciones arriba especificadas
 *
 * ERRORES DEL PROYECTO:
 * Al desconectar el Servidor repentinamente no sigue buscando conexion el cliente
 * El comando LS no puede acceder a carpetas con espacios en su nombre
 * Tener en cuenta: La fecha y hora de conexion la tiene que establecer el server, no el cliente(Por fallas horarias).
 * Ver como funciona synchronized y si es necesario implementarlo a nuestros hilos
 * Dio error el Cliente al intentar acceder a otra unidad, de F:\ a C:\
 * Mantener siempre actualizada la lista de sesiones
 * Detener servidor no funciona del lado del cliente
 * Cliente: si estamos operando la consola y se cae el server no se desconecta
 *
 * */

public class Log {
}
